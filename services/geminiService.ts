import { Tag } from "../types";

// AI Feature has been removed. This is a placeholder to prevent runtime errors.
export const parseSmartEntry = async (text: string, existingTags: Tag[]): Promise<null> => {
  console.warn("AI parsing feature is currently disabled.");
  return null;
};